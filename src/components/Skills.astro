---
// src/components/Skills.astro
import { skills } from '../data/portfolio.js';
import TechCard from './TechCard.astro';
---

<section id="skills" class="mt-20 md:mt-24 reveal">
    <div class="flex flex-col items-center mb-8 md:mb-12">
        <h2 class="text-2xl md:text-3xl font-black uppercase italic text-center bg-white text-black border-4 border-black px-6 py-2 shadow-[4px_4px_0px_0px_rgba(0,0,0,1)] -rotate-1 dark:bg-gray-800 dark:text-white dark:border-white dark:shadow-[4px_4px_0px_0px_rgba(255,255,255,0.2)]">
            Skills & Tools 🛠️
        </h2>
    </div>
    <div class="grid grid-cols-2 md:grid-cols-4 gap-3 md:gap-6">
        {skills.map(skill => <TechCard name={skill.name} color={skill.color} />)}
    </div>
</section>